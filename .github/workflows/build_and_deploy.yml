name: Build and Deploy

# on: 
#  push:
#   branches:
#    - stable

jobs:
  build:
    # No caching is used because the CMS is run on the same machine as
    # the self-hosted runner.
    name: Build Website
    runs-on: self-hosted

    steps:
    - name: Checkout main
      uses: actions/checkout@v4

    - name: Download zola
      uses: taiki-e/install-action@v2
      with:
        tool: zola@0.20.0

    - name: Set up python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Grab assets from pocketbase using glue script
      env:
        GPG_PASSPHRASE: ${{ secrets.REGEN_PASSPHRASE }}
      run: |
        pushd glue
        python -m venv venv
        source venv/bin/activate
        pip install -r requirements.txt 
        python main.py $GPG_PASSPHRASE
        deactivate
        popd

    - name: Setup bun
      uses: oven-sh/setup-bun@v2

    - name: Build with bun and zola
      run: |
        bun install
        bun run prod

    - name: Upload static files as artifact
      id: deployment
      uses: actions/upload-pages-artifact@v3
      with:
        path: dist/

  deploy:
    name: Deploy the artifact created
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: self-hosted

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v4

    - name: Deploy to github pages
      id: deployment
      uses: actions/deploy-pages@v4
